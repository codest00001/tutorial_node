      <!-- i가 짝수인것만 출력하는 if문도 추가    -->

<html>
    <head>
        <title>Forloops😎🎅</title>
    </head>
    <body>
        <ul>
            <% for(let i = 0 ; i<data.length;i++){ 
                if(i%2 == 0){ 
                %>
                <li><%= data[i]["title"]%>
                    <%= data[i]["content"]%></li>
                <% }
            } %>
        </ul>

        <ul>    
        <% data.forEach(item => { %>
            <li><%=item["title"]%>
            <%=item["content"] %></li>
            <% }) %>
       </ul>

    </body>



</html>